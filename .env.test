# Test environment variables
# These are used during test runs to prevent actual API calls

# Disable production environment
NEXT_PUBLIC_ENVIRONMENT=test

# Mock API keys for testing
NOMIC_API_KEY=test-api-key-for-testing
QD_API_KEY=test-qdrant-api-key
QD_URL=http://localhost:6333

# Use Ollama for embeddings in tests (doesn't require API key)
EMBEDDING_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=nomic-embed-text

# Disable actual API calls
NODE_ENV=test