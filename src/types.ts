export type Project = {
  title: string;
  link: string;
  description?: string;
  prize?: boolean;
  hackathon?: string;
};

export type ProjectDetails = {
  title: string;
  description: string;
  prize: boolean;
  link: string;
  hackathon: string;
};
